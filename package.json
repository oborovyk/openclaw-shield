{
  "name": "@openclaw-os/security",
  "version": "0.1.0",
  "private": true,
  "description": "OpenClaw plugin: runtime security guardrails (secret scan, prompt-injection scan, destruction guard, read-injection, bash-output scan).",
  "type": "module",
  "main": "./index.ts",
  "devDependencies": {
    "@openclaw/plugin-sdk": "workspace:*",
    "openclaw": "workspace:*"
  },
  "peerDependencies": {
    "openclaw": ">=2026.5.28"
  },
  "peerDependenciesMeta": {
    "openclaw": { "optional": true }
  },
  "openclaw": {
    "extensions": ["./index.ts"]
  }
}
